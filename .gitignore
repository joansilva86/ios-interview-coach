# macOS
.DS_Store

# Claude Code local settings (machine-specific paths)
.claude/

# Personal session logs (local-only, not shared)
logs/

# Per-candidate topic list (created by /helper on cold start, owned by /setup-session)
current_topics.txt
